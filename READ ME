This solver contains three classes.

SPACE - represents the space contained within a bound box.
Methods :   - a constructor
			- three getters (getX(), getY(), getZ())
			- fit(int[]) : tests if a given volume can fit into the space
			- bestFit(int[]) : finds the best rotation for a given volume (to maximise the number of this volume that can be fitted in the space)
			
PROBLEM - represents an optimization problem, given the space contained within the machine bound box and the dimensions of the part
Methods : 	- a constructor
			- two getters (getS(), getPart())
			- solve() : solve the problem
			
PARSER - reads the data file, and translates it into a list of Problem
Methods : 	- a constructor
			- solve() : solve the problems of the instance

HOW TO USE IT : 
Simply add the external jar to your Java project, create a new instance of Parser and execute p.solve() in your main 

Example :

******** 
import binpacking.Parser;


public class Thingy {
	public static void main(String[] args) {
		Parser p = new Parser("data.csv");
		p.solve();
	}
}

********